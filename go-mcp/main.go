// Command MultifonClient-mcp is an MCP server exposing the MultifonClient
// SDK as MCP tools.
//
// Two tools are registered:
//
//	MultifonClient_list  — list records of an entity
//	MultifonClient_load  — load a single record of an entity
//
// Each accepts an `entity` argument and an optional `query` object
// (mapped 1:1 onto the SDK's reqmatch parameter).
//
// Transports:
//
//	-transport stdio   (default) — JSON-RPC over stdin/stdout, the
//	                   standard for spawnable agent-installed servers.
//	-transport http    — streamable HTTP on -addr (default :8080),
//	                   useful when sharing one running server between
//	                   multiple agents.
//
// Generated by sdkgen go-mcp target.
package main

import (
	"context"
	"flag"
	"fmt"
	"log"
	"net/http"
	"os"

	"github.com/modelcontextprotocol/go-sdk/mcp"
	sdk "github.com/voxgig-sdk/multifon-client-sdk/go"
)

func main() {
	transport := flag.String("transport", "stdio", "transport: stdio | http")
	addr := flag.String("addr", ":8080", "listen address for http transport")
	flag.Parse()

	// Configure from the environment: MULTIFON_CLIENT_APIKEY carries the API key and
	// MULTIFON_CLIENT_BASE optionally overrides the API base URL (e.g. production).
	// Both injectable by a secrets vault. Unset -> nil config defaults.
	var opts map[string]any
	if apikey := os.Getenv("MULTIFON_CLIENT_APIKEY"); apikey != "" {
		opts = map[string]any{"apikey": apikey}
	}
	if base := os.Getenv("MULTIFON_CLIENT_BASE"); base != "" {
		if opts == nil {
			opts = map[string]any{}
		}
		opts["base"] = base
	}
	client := sdk.NewMultifonClientSDK(opts)
	server := mcp.NewServer(
		&mcp.Implementation{
			Name:    "multifon-client",
			Version: "0.0.0",
		},
		nil,
	)
	registerTools(server, client)

	switch *transport {
	case "stdio":
		if err := server.Run(context.Background(), &mcp.StdioTransport{}); err != nil {
			log.Fatalf("stdio server: %v", err)
		}
	case "http":
		handler := mcp.NewStreamableHTTPHandler(
			func(r *http.Request) *mcp.Server { return server },
			nil,
		)
		log.Printf("MCP streamable HTTP server listening on %s", *addr)
		if err := http.ListenAndServe(*addr, handler); err != nil {
			log.Fatalf("http server: %v", err)
		}
	default:
		fmt.Fprintf(os.Stderr, "unknown transport %q (want stdio | http)\n", *transport)
		os.Exit(2)
	}
}
